/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com', 'source.unsplash.com', 'firebasestorage.googleapis.com'],
    },
}

module.exports = nextConfig
